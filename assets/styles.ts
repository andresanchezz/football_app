import { StyleSheet } from "react-native";

const buttonStyles = StyleSheet.create({

    button: {
        marginVertical: 10,
        backgroundColor: 'red',
        paddingVertical: 15,
        borderRadius: 4
    },
    textButton: {
        color: 'white',
        textAlign: 'center',
        fontWeight: 600
    },
    textButtonBlack: {
        textAlign: 'center',
        fontWeight: 600
    }

})


export default { buttonStyles }